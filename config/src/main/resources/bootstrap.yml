spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/igorKryvenko/spring-cloud-configs
          clone-on-start: true
        bootstrap: true
      fail-fast: true